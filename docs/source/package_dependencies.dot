digraph package_dependencies {
    graph [rankdir=TB]

    /* Node Types
        When multiple adjustments are present, order is label, style, shape

        Direct installation: `style="bold"`
        
        From 'requirements.txt`: `shape="component"`
        From AWS Glue: `shape="cylinder"`
        From Dockerfile: `shape="folder"`
        Dependencies: `shape="box"`
    */
    // Dependency relationships go from installed package to dependent package and are listed under the former

    awscli [
        label="\N==1.23.5"
        style="bold"
        shape="cylinder"
    ]

    beautifulsoup4 [
        label="\N==4.12.3"
        style="bold"
        shape="component"
    ]
    beautifulsoup4 -> soupsieve

    blinker [
        label="\N==1.9.0"
        shape="box"
    ]

    boto3 [
        label="\N==1.34.69"
        style="bold"
        shape="component"
    ]
    boto3 -> botocore
    boto3 -> jmespath
    boto3 -> s3transfer

    botocore [
        label="\N==1.34.162"
        style="bold"
        shape="cylinder"
    ]

    Bottleneck [
        label="\N==1.4.2"
        style="bold"
        shape="box"
    ]

    click [
        label="\N==8.1.8"
        shape="box"
    ]

    colorama [
        label="\N==0.4.6"
        style="bold"
        shape="cylinder"
    ]

    docutils [
        label="\N==0.21.2"
        style="bold"
        shape="cylinder"
    ]

    "et-xmlfile" [
        label="\N==2.0.0"
        shape="box"
    ]

    Flask [
        label="\N==3.0.3"
        style="bold"
        shape="component"
    ]
    Flask -> blinker
    Flask -> click
    Flask -> itsdangerous
    Flask -> Jinja2
    Flask -> Werkzeug

    "Flask-SQLAlchemy" [
        label="\N=="3.1.1"
        style="bold"
        shape="component"
    ]
    "Flask-SQLAlchemy" -> Flask
    "Flask-SQLAlchemy" -> SQLAlchemy

    "Flask-WTF" [
        label="\N==1.2.1"
        style="bold"
        shape="component"
    ]
    "Flask-WTF" -> Flask
    "Flask-WTF" -> itsdangerous
    "Flask-WTF" -> WTForms

    fsspec [
        label="\N==2024.5.0"
        style="bold"
        shape="component"
    ]

    gevent [
        label="\N==24.11.1"
        style="bold"
        shape="folder"
    ]

    graphviz [
        label="\N==0.20.3"
        style="bold"
        shape="component"
    ]

    gunicorn [
        label="\N==23.0.0"
        style="bold"
        shape="folder"
    ]

    iniconfig [
        label="\N==2.0.0"
        shape="box"
    ]

    itsdangerous [
        label="\N==2.2.0"
        shape="box"
    ]

    Jinja2 [
        label="\N==3.1.5"
        shape="box"
    ]

    jmespath [
        label="\N==1.0.1"
        style="bold"
        shape="cylinder"
    ]

    lxml [
        label="\N==5.2.1"
        style="bold"
        shape="component"
    ]

    mysql [
        label="\N==0.0.3"
        style="bold"
        shape="component"
    ]
    mysql -> mysqlclient

    mysqlclient [
        label="\N==2.2.4"
        style="bold"
        shape="component"
    ]

    numba [
        label="\N==0.61.0"
        style="bold"
        shape="box"
    ]

    numexpr [
        label="\N==2.10.2"
        style="bold"
        shape="box"
    ]

    numpy [
        label="\N==2.1.3"
        shape="box"
    ]

    openpyxl [
        label="\N==3.1.2"
        style="bold"
        shape="component"
    ]
    openpyxl -> "et-xmlfile"

    packaging [
        label="\N==24.2"
        shape="box"
    ]

    pandas [
        label="\N==2.2.2"
        style="bold"
        shape="component"
    ]
    pandas -> numpy
    pandas -> "pandas[parquet]"
    pandas -> "pandas[performance]"
    pandas -> "python-dateutil"
    pandas -> pytz
    pandas -> tzdata

    "pandas[parquet]" [
        shape="component"
    ]
    "pandas[parquet]" -> pyarrow

    "pandas[performance]" [
        shape="component"
    ]
    "pandas[performance]" -> Bottleneck
    "pandas[performance]" -> numba
    "pandas[performance]" -> numexpr

    pluggy [
        label="\N==1.5.0"
        shape="box"
    ]

    pyarrow [
        label="\N==19.0.1"
        style="bold"
        shape="box"
    ]

    pyasn1 [
        label="\N==0.4.8"
        style="bold"
        shape="cylinder"
    ]

    PyInputPlus [
        label="\N==0.2.12"
        style="bold"
        shape="component"
    ]
    PyInputPlus -> PySimpleValidate
    PyInputPlus -> stdiomask

    PyMySQL [
        label="\N==1.0.2"
        style="bold"
        shape="component"
    ]

    PySimpleValidate [
        label="\N==0.2.12"
        shape="box"
    ]

    pytest [
        label="\N==8.2.0"
        style="bold"
        shape="component"
    ]
    pytest -> iniconfig
    pytest -> packaging
    pytest -> pluggy

    "pytest-dependency" [
        label="\N==0.6.0"
        style="bold"
        shape="component"
    ]
    "pytest-dependency" -> pytest
    "pytest-dependency" -> setuptools

    "pytest-session2file" [
        label="\N==0.1.11"
        style="bold"
        shape="component"
    ]
    "pytest-session2file" -> pytest

    "python-dateutil" [
        label="\N==2.9.0.post0"
        style="bold"
        shape="cylinder"
    ]

    pytz [
        label="\N==2025.1"
        shape="box"
    ]

    PyYAML [
        label="\N==5.4.1"
        style="bold"
        shape="cylinder"
    ]

    requests [
        label="\N==2.31.0"
        style="bold"
        shape="component"
    ]

    "requests-toolbelt" [
        label="\N==1.0.0"
        style="bold"
        shape="component"
    ]

    rsa [
        label="\N==4.7.2"
        style="bold"
        shape="cylinder"
    ]

    s3fs [
        label="\N==2024.5.0"
        style="bold"
        shape="component"
    ]

    s3transfer [
        label="\N==0.10.4"
        style="bold"
        shape="cylinder"
    ]

    setuptools [
        label="\N==75.8.0"
        shape="box"
    ]

    six [
        label="\N==1.17.0"
        style="bold"
        shape="cylinder"
    ]

    Sphinx [
        label="\N==7.3.7"
        style="bold"
        shape="component"
    ]

    "sphinx-autobuild" [
        label="\N==2024.4.16"
        style="bold"
        shape="component"
    ]

    "sphinx-autodocgen" [
        label="\N==1.3"
        style="bold"
        shape="component"
    ]

    SQLAlchemy [
        label="\N==2.0.29"
        style="bold"
        shape="component"
    ]

    stdiomask [
        label="\N==0.0.6"
        shape="box"
    ]

    tzdata [
        label="\N==2025.1"
        shape="box"
    ]

    urllib3 [
        label="\N==2.3.0"
        style="bold"
        shape="cylinder"
    ]

    Werkzeug [
        label="\N==3.1.3"
        shape="box"
    ]

    WTForms [
        label="\N==3.1.2"
        style="bold"
        shape="component"
    ]
}